import 'package:flutter/material.dart';

class AppColors {
  static const cardIcon = Color.fromRGBO(0, 55, 103, 1);
  static const infoRowTitle = Color.fromRGBO(51, 65, 85, 1);
  static const cardNotificationText = Colors.orange;
  static const cardContentBg = Color.fromRGBO(243, 246, 250, 1);
}
